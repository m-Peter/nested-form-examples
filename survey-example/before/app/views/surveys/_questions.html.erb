<div class="fields">
  <p>
    <%= f.label :content, "Question" %><br>
    <%= link_to_remove_fields "remove", f %><br />
    <%= f.text_area :content, :cols => 30, :rows => 2 %>
  </p>

  <%= f.fields_for :answers do |builder| %>
    <%= render "answers", :f => builder %>
  <% end %>

  <p><%= link_to_add_fields "Add Answer", f, :answers %></p>

</div>